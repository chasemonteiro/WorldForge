name: Deploy Grace Fidget Tuning

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-grace-fidget.yml'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tarnished-covenant-assets
          fetch-depth: 0
      - name: Validate Grace fidget source
        run: |
          python -m py_compile tarnished-covenant/build-grace-fidget.py
          python -m py_compile tarnished-covenant/build-grace-animation-stability.py
      - name: Apply Grace fidget tuning
        run: |
          python tarnished-covenant/build-grace-fidget.py
          python tarnished-covenant/build-launch-refresh.py
      - name: Validate live app
        run: |
          grep -q 'Grace fidget tuning' tarnished-covenant/index.html
          grep -q 'tc-grace-tap-count' tarnished-covenant/index.html
          grep -q 'tcIncrementGraceTapCounter(btn)' tarnished-covenant/index.html
          grep -q 'TC_GRACE_TAP_COOLDOWN_MS=100' tarnished-covenant/index.html
          grep -q 'TC_GRACE_FAVOR_ODDS=5000' tarnished-covenant/index.html
          if grep -q 'TC_GRACE_ART_FRAMES' tarnished-covenant/index.html; then exit 1; fi
          python tarnished-covenant/test-regression-invariants.py
          python - <<'PY'
          from pathlib import Path
          import re
          html=Path('tarnished-covenant/index.html').read_text()
          scripts=re.findall(r'<script(?:[^>]*)>(.*?)</script>',html,flags=re.S)
          if not scripts: raise SystemExit('No inline script found')
          Path('/tmp/tc.js').write_text('\n'.join(scripts))
          PY
          node --check /tmp/tc.js
      - name: Commit live app
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tarnished-covenant/index.html
          if git diff --cached --quiet; then echo 'No changes to commit.'; else git commit -m 'Add Grace tap counter and livelier wisps'; git push origin HEAD:tarnished-covenant-assets; fi
