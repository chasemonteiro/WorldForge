name: Deploy Grace Animation Stability

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired one-shot workflow. Use the normal Tarnished Covenant build workflow.'
