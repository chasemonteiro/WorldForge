name: Deploy Final Covenant Refresh Handler Fix

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-refresh-handler-final.yml'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tarnished-covenant-assets
          fetch-depth: 0
      - name: Finalize refresh handler
        run: |
          python tarnished-covenant/build-refresh-handler-final.py
          python tarnished-covenant/build-launch-refresh.py
      - name: Validate live refresh handler
        run: |
          python tarnished-covenant/test-regression-invariants.py
          python tarnished-covenant/test-final-release-invariants.py
          if grep -q 'tcUseCovenantBoonBefore' tarnished-covenant/index.html; then exit 1; fi
          python - <<'PY'
          from pathlib import Path
          import re
          html=Path('tarnished-covenant/index.html').read_text()
          scripts=re.findall(r'<script(?:[^>]*)>(.*?)</script>',html,flags=re.S)
          if not scripts: raise SystemExit('No inline scripts found')
          Path('/tmp/tc-refresh-final.js').write_text('\n'.join(scripts))
          PY
          node --check /tmp/tc-refresh-final.js
      - name: Commit live fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tarnished-covenant/index.html
          if git diff --cached --quiet; then
            echo 'No live changes to commit.'
          else
            git commit -m 'Remove blocking Covenant refresh wrapper'
            git push origin HEAD:tarnished-covenant-assets
          fi
